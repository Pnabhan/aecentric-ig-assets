# Digital Twin videos

Generated HeyGen Digital Twin video files, one folder per video id, matching
the pattern used by `posts/` for carousel images:

```
videos/<video-id>/video.mp4
```

Hosted via raw.githubusercontent.com so Instagram's API can fetch them for
Reels publishing. See `HEYGEN_PIPELINE.md` in the automation repo for the
full pipeline this feeds.
